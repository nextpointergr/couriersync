<?php

namespace App\CourierMaps;

use Nextpointer\CourierSync\Contracts\CourierMapper;

class {{class}} implements CourierMapper
{
    public function map(array $row): array
    {
        return [
            'voucher'  => isset($row[0]) ? trim($row[0]) : null,
            'amount'   => isset($row[1]) ? (float) str_replace(',', '.', $row[1]) : 0,
            'date'     => $row[2] ?? null,
            'customer' => isset($row[3]) ? trim($row[3]) : null,
        ];
    }
}